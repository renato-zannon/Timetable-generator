<ul>
<% @groups.sort_by { |g| g.discipline.name}.each do |group| %>
  <li><%= link_to "#{group.discipline.name} - #{group.name}", group %></li>
<% end %>
</ul>
<%= link_to "New group", new_group_path %>
