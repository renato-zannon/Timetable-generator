<%= form_tag generate_best_timetable_path, :method => :post do %>
  <% @disciplines.each do |discipline| %>
    <%= label_tag     "disciplines_#{discipline.code}", "#{discipline.code} - #{discipline.name}" %>
    <%= check_box_tag "disciplines[#{discipline.code}]" %><br />
  <% end %>
  <%= submit_tag 'Submit' %>
<% end %>
